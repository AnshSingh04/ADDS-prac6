#include "Mutator.h"
#include <iostream>
#include <cmath>
#include <string>
using namespace std;

Mutator::Mutator(){}

Individual *Mutator::mutate(Individual i,int k) {
    Individual *ptr;
    ptr = new Individual(1);
    return ptr;
}
